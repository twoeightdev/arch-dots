#!/bin/sh
# Search music playlist in dmenu and play it on mpd
track=`mpc playlist -f "%position% - %title% - %artist% - %album%" | dmenu -i -fn Monospace-12 -nb "#22262e" -sb "#bf616a" -nf "#d8dee9" -l 10 -p "Track"` && mpc play "${track%% *}"
echo "$track"
