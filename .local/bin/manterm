#!/bin/sh
# Search manpage in dmenu
manpage=$(man -k . | dmenu -i -fn Monospace-12 -nb "#22262e" -sb "#bf616a" -nf "#d8dee9" -l 10 | awk '{ print $1}') && if [ -z "$manpage" ]; then exit; else $TERMINAL -e man "$manpage" & fi
